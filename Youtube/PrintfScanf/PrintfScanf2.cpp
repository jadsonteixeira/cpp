#include <iostream>
#include <cstdlib>
#include <stdio.h>
#include <math.h>

using namespace std;

int main() {
    system("cls");

    float pi = M_PI;
    int num = 1;

    printf("Valor de PI: %.2f", pi); 

    // completando com 0s a esquerda
    printf("Numero: %.07d", num);

    return 0;
}